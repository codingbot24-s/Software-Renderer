

/*
    Implement your own framebuffer in CPU memory, and each frame copy its contents to the window so 
    that changing pixels in the framebuffer changes what appears on the screen.
*/

#include "sdl_init.h"

int main () {
    create_sdl_window();
}
