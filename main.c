

// TODO: we need to organize our code

/*
   Implement a function that sets the color of a single pixel in the framebuffer using integer (x, y) coordinates, 
   with proper bounds checking to prevent writing outside the framebuffer.
*/

#include "sdl_init.h"

int main () {
   create_sdl_window();
}
